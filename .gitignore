# Dependencies
node_modules/

# Build output
dist/
target/

# Temp files
.temp/
_tmp_*
*.tmp

# Release builds
releases/

# Cordova/Weex platform builds
platforms/

# Plugins
plugins/

# IDE
.vscode/
.idea/
*.swp
*.swo
*.iml

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# AI tools
.claude/
.codebuddy/
.code-review-graph/
.gemini/
.qoder/
.trae/
.workbuddy/
.mcp.json
opencode.jsonc
.brooks-lint-history.json

# 技术文档类 md（参考文档，不纳入版本控制）
参考资料/

# 临时脚本和生成的静态文件
generate_static.cjs
publish_article.cjs
article_*.html

# 覆盖率报告
jacoco-0.8.15/

# 环境变量配置与本地覆盖（含敏感密钥，禁止提交）
.env
.env.*
!.env.example
# Spring Boot 本地凭据覆盖文件（真实口令只允许存在本地磁盘）
application-local.yml
application-local.yaml
application-*-local.yml
application-*-local.yaml
*-local.properties
# 证书与密钥
*.pem
*.p12
*.jks
*.key

# 本地监控组件（Prometheus/Loki/Grafana/Promtail 及其运行数据、安装包，均不入库）
monitoring/

# 时序数据库运行数据（Loki TSDB chunks/wal/compactor/shipper 等）
data/

# Promtail 日志位置记录
positions.yaml

# 个人/临时文件
*.pdf
.vite_test_dist/

# 个人参考截图与资料（不纳入版本控制，保留在磁盘）
/*.png
docs/code-review-quality-report.md
docs/interview-core-business-points.md
docs/juejin-design-reference.md
docs/resume-project-zhuri-coding.md
cdp-browser.js

# 掘金数据导入工具与生成产物（一次性数据脚本，不入库）
juejin-import/

# ============================================================
# 求职材料与本地联调信息（**请勿公开上传**）
# ------------------------------------------------------------
# · AGENTS.md 含本地联调用的沙箱账号/密码等敏感信息 → 必须排除
# · 面试准备文档若公开，会被面试官检索到（等于把准备套路交底）→ 排除
# · 简历材料（含电话/邮箱）同理；*.pdf 已在上方排除
# ============================================================
AGENTS.md
CLAUDE.md
resume/
docs/interview-*.md
docs/resume-project-*.md

# 本地临时输出与日志目录
_*.txt
nacos-logs/

# ---------- 截图目录（README 已改用 OSS 图床，本地留档不入库）----------
screenshots/

# ---------- docs/ 文档目录（项目运行不依赖，仅本地阅读）----------
# 说明：docs/ 下是设计文档、AI 增强记录与个人求职材料，代码与运行链路均不引用它。
# 上方已单独排除面试/简历类文档；此处整目录排除（默认不公开任何文档）。
docs/

# 【备选方案】若想把 4 份 AI 设计文档作为作品展示（面试加分项），
# 把上面的 `docs/` 换成下面这组规则（先放行目录、再逐份放行）：
# docs/*
# !docs/ai-rag-qa-design.md
# !docs/ai-agent-precheck-design.md
# !docs/ai-aigc-content-integrity-design.md
# !docs/ai-commercialization-roadmap.md
