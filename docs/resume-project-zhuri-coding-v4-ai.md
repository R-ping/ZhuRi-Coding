# 逐日 Coding · 简历版项目经历 v4（AI 版·包含关系，可直接粘贴）

> 版本 v4（2026-09-09）：基于"Spring AI 功能增强"落库后的**当前源码**重写，定位为 **AI 应用 × Java 后端**（包含关系，不拆项目）。
>
> 用户拍板的组合口径：
>
> - 简历要点总量 **7 条封顶**；结构 = **Java 4 条稳定盘 + AI 3 条**（主推/精简两版一致；曾试验 AI 4 条，第 4 条区分度不足、Spring AI 亮点支撑有限 → 收敛回 3 条，安全/成本治理要点转面试口头补充）；
> - AI 版可同时投普通 Java 岗（AI 是差异化亮点）与 AI 应用开发岗（后端底子=相对纯框架应届的优势）；
> - 不拆两个项目（同一代码库/同一时间段硬拆会穿帮，且稀释 Java 深度条）。
>
> 相对 v3（主推 10 条）的变化：  
> ① 总量压缩到 7 条，Java 侧 6→4：保留发布状态机/事件总线/支付/双Token+信任链，**双等级成就、限流降为面试口述扩展**（并入第 4 条或对话补充，不再单列）；② AI 侧 0→3 条（v3 仅"AI 审核"作为 Java 链路一部分）；③ 技术栈随 Spring AI 全迁更新：Boot 3.5.16 / SCA 2025 / Spring AI 1.1.8 / qwen 模型 / pgvector。
>
> 二次修订（2026-09-09）：按用户评审意见，AI 条目**去除"开发过程/演进"与"接入层/排坑"叙事**（如"自研 ReAct 演进到 Spring AI""统一 OpenAI compatible 协议并框架化""适配专属网关协议差异"——前者是开发过程，后者本质是拉 spring-ai-starter-model-openai 依赖 + 排坑日志，任何使用者都会做、无区分度），统一改写为**最终架构 / 设计决策 / 横向能力**表述；该主题重塑为"LLM 应用安全与成本治理"（安全横切层 + 多维限频 + 缓存降本）。删除 qwen 视觉封面弱表述；接入层细节仅在被追问时口头补充（口径见第三部分 AI-3 行）。
>
> 三次修订（2026-09-09）：AI 条数由 4 收敛为 3——撤销"LLM 应用安全与成本治理"独立条（Prompt 注入声明式横切 / AI 接口多维限频 / 高频缓存双控成本），该主题属于横向能力且单薄，转面试口头补充（口径见第三部分 AI-3 行"补充口头亮点"）；主推与精简统一为 **Java 4 + AI 3 = 7 条**。
>
> ⚠️ 待用户定稿两处（文档不替你做决定，正文先按推荐口径写）：
>
> - **时间线**：PDF 简历写 `2025.09-至今`，v3 文档写 `2024.06-2024.09` → 本文按 PDF 口径 `2025.09-至今`，投递前统一；
> - **框架口径**：真实链路是 DashScope 专属网关 + 自研 OpenAI compatible 通道演进到 **Spring AI**（LangChain4j 仅评估 beta 未采用）→ 个人优势与面试口径一律讲 Spring AI；若 JD 点名 LangChain4j，口径为"概念等价 + 手写编排器更可控"。

---

## 一、可直接粘贴版（主推 Java 4 + AI 3 = 7 条）

**逐日 Coding｜Java 开发（AI 应用方向）｜2025.09-至今**

**项目描述：** 面向程序员的内容社区与创作者变现平台（头条/掘金/知识付费综合体）。业务侧覆盖内容发布与审核、搜索索引、双等级激励与成就、课程购买/打赏、签到兑换、站内信 IM；AI 侧以同一套后端基座承载大模型应用：RAG 社区问答、AI 发布助手（Agent 工具调用）、文章 AI 速览、UGC 评论治理。按业务拆 user/content/search/reward/notification 五个微服务 + 统一网关（各服务独立库），独立完成核心链路架构设计与 90%+ 核心模块。

**技术栈：** Java 17 · Spring Boot 3.5.16 · Spring Cloud Alibaba 2025（Nacos/OpenFeign/Gateway）、MyBatis-Plus、MySQL 8、Redis+Redisson、Elasticsearch、**PostgreSQL+pgvector 向量检索**、**Spring AI 1.1.8（OpenAI compatible）**、**qwen 大模型（文本/Embedding）**、**SSE 流式**、阿里云 OSS、支付宝沙箱、STOMP WebSocket、Zipkin/JaCoCo

### AI 侧（3 条，面试主线）

1. **自研 RAG 社区问答 + 单篇问答（检索质量 + 成本工程）**：文章经 Embedding 入 pgvector(1024)；口语问题先 Query Rewrite 再宽召回（15 候选）→ LLM Rerank 精排到 TopK → 答案逐句标注 `[n]` 引用、前端可点击溯源原文；SSE 逐字流式 + 最近 6 轮会话记忆滑窗解析指代；另做"只答本文"的文章级单篇问答与 AI 摘要，摘要 Redis 缓存 24h、长文截断 1.2 万字符控 token 成本——同一 LLM 基座按"多篇检索 / 单篇收敛"两种上下文形态提供能力。
2. **AI 发布助手（LLM Agent + Function Calling 驱动）**：发布前预检由 Agent 编排——把「内容安全规则引擎、pgvector 相似内容检索」注册为 Spring AI 原生工具，模型自主决策调用后输出违规判定/质量分/标签/摘要的结构化预检报告；规则类与检索类能力收敛为本地工具（相似检索排除自身、≥0.72 阈值预警），让模型做编排与生成、而非凭记忆作答，兼顾准确与可控；工具调用或模型异常时自动降级直答，发布主链路不阻塞。
3. **AI 内容审核与 UGC 治理体系**：发布内容 AI 审核通过按质量分回馈作者成长值；UGC 评论/沸点评论"先展示后审核"——任务落库可靠队列（崩溃由定时补偿拉起），CAS 抢占防重复、指数退避重试、超限降级放行不误伤；红线违规物理删除并通知，温和违规（引战/阴阳/软广）经 LLM 二次判定折叠（文章评论折叠条"仅自己可见"可申诉、沸点全局隐藏），一级评论折叠整树无痕、二级/回复逐条治理，形成"平台不当裁判、温和可逆"的治理语义。

### Java 侧（4 条，稳定盘）

1. **内容"发布→AI 审核→定时上线→搜索索引"最终一致**：审核通过后按发布时刻经 Redisson 延迟队列调度（无 MQ）；到点先落本地消息表 → 条件更新置发布态 → 一步携带发布态同步 ES → 完成清理；消息表用**单 status 状态机**（DB 置位失败幂等自愈、ES 同步失败 20s 定时扫描重试、超限死信），DB 与搜索双写以"本地消息表+定时补偿"收敛，不引入 MQ。
2. **自研行为事件总线 + @Order 后置处理器责任链**：点赞/关注/评论等一次行为按类型路由，主操作落库后有序触发等级积分、文章热度、站内信通知、成就解锁、统计等副作用，处理器独立降级互不拖垮；"行为记录门控 + 唯一索引"双保险防重复加分/重复通知；文章热度单条原子 SQL 重算（点赞×3+评论×3+收藏×6+浏览）消除读改写竞态。
3. **统一支付与资损兜底（课程购买 + 文章打赏双变现）**：金额全部服务端计算（折扣码+抽奖 5 折券；打赏限 1~10000 元并拦截自赏）；异步回调 RSA2 验签+app_id+实付金额一致多重校验后，以"条件更新原子抢占订单状态"幂等放权/入账，重复通知与并发回调不重复发奖/不重复流水；Redisson 延迟队列超时关单，对"支付成功但已关单"竞态自动退款（订单号幂等键防重复退，失败入重试任务）；课程 7:3 月度结算、打赏按流水结算，结算表唯一索引防重。
4. **双 Token 无感续期 + 网关信任链 + 分层限流**：JWT Access(1h)+UUID Refresh(7d)，刷新用 Redis Lua 原子"取即删"轮换 refresh_token，并发仅一人成功；网关校验失败返 444 驱动前端自动刷新重放、登出服务端吊销；网关与下游 HMAC 内部签名、身份仅取网关透传（防伪造 userId 串号），ThreadLocal 请求结束即清理；网关 IP 固定窗口 + 自定义 @RateLimit（Redis+Lua 滑动窗口、全局/IP/用户多维叠加、NOSCRIPT 自愈、可配降级）+ 拦截器令牌桶三层防刷。

---

## 二、篇幅紧张精简版（Java 4 + AI 3 = 7 条，每条一句话浓缩，适合一页纸）

**逐日 Coding｜Java 开发（AI 应用方向）｜2025.09-至今**

**项目描述 / 技术栈：** 同上（可截断技术栈为：Boot 3.5 · Spring Cloud Alibaba · MyBatis-Plus · MySQL · Redis/Redisson · ES · pgvector · **Spring AI + qwen** · OSS · 支付宝沙箱）

1. **自研 RAG 问答 + AI 速览（检索→重排→引用→流式）**：文章向量化入 pgvector，口语问题 Query Rewrite → 宽召回 + LLM Rerank → 回答带 `[n]` 引用溯源、SSE 逐字流式 + 多轮记忆；AI 摘要 Redis 缓存 24h 控成本；另支持"只答本文"单篇问答。
2. **AI 发布助手：LLM Agent + Function Calling**：发布预检由 Agent 编排——内容安全规则引擎与 pgvector 相似检索注册为 Spring AI 原生工具，模型自主调用后输出违规判定/质量分/标签/摘要；规则类与检索类能力收敛为本地工具避免模型凭记忆作答，异常自动降级直答、发布不阻塞。
3. **AI 内容审核与 UGC 治理**：发布 AI 审核 fail-closed（异常拒放行）+ 质量分回馈作者；评论"先展示后审核"可靠队列（任务落库/定时补偿/CAS 防重/超限降级放行），红线违规删除、温和违规 LLM 折叠，文章/沸点/回复全覆盖。
4. **发布→上线→搜索最终一致**：Redisson 延迟队列调度 + 本地消息表单 status 状态机（DB 失败自愈 / ES 失败 20s 扫描重试 / 超限死信），无 MQ 的 DB 与 ES 双写最终一致。
5. **行为事件总线 + @Order 责任链**：一次行为有序触发积分/热度/通知/成就，处理器独立降级；行为记录门控 + 唯一索引防重复，热度原子 SQL 重算。
6. **支付资损兜底**：服务端计价 + RSA2 验签 + 条件更新幂等放权；超时关单 + "已关单却支付成功"自动退款（幂等键），7:3 月度结算防重；打赏限额禁自赏联动经验与通知。
7. **双 Token + 网关信任链 + 分层限流**：Redis Lua 原子轮换防竞态、444 驱动刷新重放；HMAC 内部签名防伪造 userId；IP 固定窗口 / @RateLimit Lua 滑动窗口 / 令牌桶三层防刷。

---

## 三、代码证据与面试口径备注（每条可被深挖到文件/机制）

| #    | 面试可展开的证据点                                                                                                                                                                                                                                               | 对应代码位置                                                                                                                                                  |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AI-1 | Query Rewrite→宽召回 15→LLM Rerank→[n] 引用；SSE 事件 delta/done/error 协议；多轮 memory 滑窗(≤12 条、每条截断)；摘要 key `ai:article:summary:{id}` TTL 24h；正文 Markdown 直入上下文、长文截断 1.2 万字符                                                                                      | `AiAskServiceImpl`（REWRITE/RERANK/RECALL_TOPK）、`AiAskController.askStream`、`ArticleQaServiceImpl`                                                       |
| AI-2 | Agent 编排如何落：工具=确定性规则（敏感词/违法类型）+ 向量检索（排除自身、≥0.72 预警），模型只做编排与生成并输出结构化终态；异常自动降级直答保发布链路。面试口径：开发过程不展开，统一答『先以轻量通道完成能力验证(POC)，再框架化收敛协议与工具维护成本』，最终态 Spring AI 原生 Function Calling | `AgentRunner`(Spring AI 版)、`ai/agent/tools`、`PublishAssistantServiceImpl` |
| AI-3 | 可靠队列：`ap_comment_audit_task` 与 `ap_pins_comment_audit_task`（两张表因源表各自自增不能共用唯一键）；CAS PENDING→PROCESSING；退避 60s×2^n 封顶 5 次降级放行；折叠语义：文章 is_hidden=1 本人可见灰条、沸点全局隐藏、一级折叠整树无痕；回复已补入审核链                                                                         | `CommentAuditService`/`PinsCommentAuditService`（extends AbstractAuditService）、`CommentAuditRecoveryTask`、`ApCommentServiceImpl`/`PinsQueryService` 读侧过滤。补充口头亮点（不单列成条）：Prompt 注入三层防御沉淀为声明式 Advisor 一处挂载全链路生效、AI 接口登录态+USER/IP 多维限频，即原"安全与成本治理"要点 |
| J-4  | article_event 单 status 状态机常量（INIT/DB_SET_FAIL/ES_SYNC_FAIL/DONE），删除旧双状态位与事件监听双写                                                                                                                                                                         | `ApArticleEventServiceImpl`、`ArticleConstants`                                                                                                          |
| J-5  | BehaviorEventBus + List<Handler> @Order；行为记录门控+唯一索引；热度原子 SQL                                                                                                                                                                                            | `BehaviorEventBus`、`comment/behavior`、`ApArticleMapper`                                                                                                 |
| J-6  | 条件更新 `UPDATE … WHERE status=待支付` 幂等；退款幂等键；结算唯一索引                                                                                                                                                                                                        | content 支付服务、reward 结算                                                                                                                                  |
| J-7  | Redis Lua 取即删轮换；网关 HMAC 内部签名；@RateLimit 注解实现                                                                                                                                                                                                            | user 认证、网关过滤器、common 限流                                                                                                                                 |

> 面试主战线建议：**AI 1（RAG 检索质量）→ AI 3（审核与治理）→ J 4（发布一致性）→ J 6（支付兜底）**，其余作扩展亮点；AI 岗则前置 AI 1~3，Java 岗前置 J 4/5/6。
> 2 分钟项目 STAR 锚点：面试官问"项目做了什么"→ 一句话业务 + "AI 是第二引擎" + 主链路一致性 + 一个真实问题的解决实例收尾。

---

## 四、投递双版排序（同一份内容，两种开头）

- **投 Java 岗**：按"一"的顺序直接贴（AI 在后半场当亮点），求职意向写"Java 开发"。
- **投 AI 应用开发岗**：把"一、AI 侧 3 条"整体提到 Java 侧之前（顺序 AI-1~3 → J-4~7），项目描述首句强调"大模型应用已上线于真实业务（RAG 问答/Agent/内容治理）"，求职意向写"Java/AI 应用开发"；技术栈把 Spring AI、pgvector、SSE 提到最前。
