-- ============ 清理没有任何小节的空课程（软删除） ============
-- 目标：移除公开课程列表中"只有课程信息、疑似测试/占位、一个小节都没有"的空课程。
-- 处理方式：软删除（is_deleted=1），保留 ap_course_order 等关联数据可追溯，且不再展示。
-- 说明：ap_course_reading_progress 按 chapter_id 关联，空课程无章节故无进度记录；ap_course_chapter 为空。
USE `leadnews_article`;

UPDATE `ap_course`
SET `is_deleted` = 1
WHERE `is_deleted` = 0
  AND `id` IN (
    -- 已上架(status=9)但无章节：会出现在公开课程列表却无内容
    2,                                   -- 如何写一本掘金小册
    3,                                   -- Java入门教程
    4,                                   -- 实战：手把手做一个MiniCodeAgent
    2088642484839063553,                 -- 稀土掘金社区产品功能深度剖析
    -- 草稿(status=0)未命名课程/小册：无标题无内容
    2088605792027414529,
    2088621815145304066,
    2088625941350584322,
    2088630950968561665,
    2089278760776810497,
    2089278825473949697,
    2089279063131602945,
    2089279269193564162,
    2089582967828418562
  );