<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuri.coding.content.mapper.article.ApArticleEventMapper">
  <!-- 2026-09-26 清理：移除 minio_status / es_status / pub_status 三个已废弃列。
       状态机早已收敛为单 status，这三列此前仅被本文件的两条旧 SQL 引用（实体字段也因此保留着），
       属"迁移只做了一半"的残留 —— 数据折算做了，冗余结构没清。 -->
  <insert id="insertArticleEvent">
    insert into article_event (article_id, status, create_time, update_time, retry_count, max_retry_count, retry_time, parameter)
    values (#{articleEvent.articleId}, #{articleEvent.status}, #{articleEvent.createTime}, #{articleEvent.updateTime},
            COALESCE(#{articleEvent.retryCount}, 0), COALESCE(#{articleEvent.maxRetryCount}, 5), #{articleEvent.retryTime}, #{articleEvent.parameter})
  </insert>


  <update id="updateArticleEvent">
    update article_event
    <set>
      <if test="articleEvent.status != null">
        status = #{articleEvent.status},
      </if>
      <if test="articleEvent.retryCount != null">
        retry_count = #{articleEvent.retryCount},
      </if>
      <if test="articleEvent.retryTime != null">
        retry_time = #{articleEvent.retryTime},
      </if>
      <if test="articleEvent.updateTime != null">
        update_time = #{articleEvent.updateTime},
      </if>
    </set>
    where article_id = #{articleEvent.articleId}
  </update>
  <delete id="deleteArticleEvent">
    delete from article_event where article_id in
    <foreach collection="articleIds" item="articleId" open="(" separator="," close=")">
      #{articleId}
    </foreach>
  </delete>
</mapper>