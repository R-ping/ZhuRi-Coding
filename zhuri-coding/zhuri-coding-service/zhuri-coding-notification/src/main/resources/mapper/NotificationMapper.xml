<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuri.coding.notification.mapper.NotificationMapper">

    <select id="selectByTypeAndCursor" resultType="com.zhuri.coding.model.notification.pojos.Notification">
        SELECT * FROM notifications
        WHERE user_id = #{userId}
          AND type = #{type}
          <if test="cursor != null and cursor > 0">
              AND id &lt; #{cursor}
          </if>
        ORDER BY id DESC
        LIMIT #{size}
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM notifications
        WHERE user_id = #{userId} AND is_read = 0
    </select>

    <select id="countUnreadGroupByType" resultType="map">
        SELECT type as type, COUNT(*) as count FROM notifications
        WHERE user_id = #{userId} AND is_read = 0
        GROUP BY type
    </select>

    <update id="markAllRead">
        UPDATE notifications SET is_read = 1
        WHERE user_id = #{userId} AND is_read = 0
    </update>

    <select id="countUnreadByType" resultType="int">
        SELECT COUNT(*) FROM notifications
        WHERE user_id = #{userId} AND type = #{type} AND is_read = 0
    </select>

    <update id="markTypeRead">
        UPDATE notifications SET is_read = 1
        WHERE user_id = #{userId} AND type = #{type} AND is_read = 0
    </update>
</mapper>